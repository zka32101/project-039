{
  "name": "anshinmichi-functions",
  "version": "0.1.0",
  "private": true,
  "description": "あんしんみち Cloud Functions（経路探索・影スコアバッチ・モデレーション）",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "seed": "node seed/seedRoadNetwork.js",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^6.0.1"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.3.0"
  }
}
